<!--meta
{
  "title": "Getting Started",
  "description": "Open your first document and learn how the reader is laid out.",
  "assumes": [],
  "next": ["Guides/concepts/graph-model", "Guides/markdown-kitchen-sink"]
}
-->

# Getting Started

Welcome to the **Web Document Tool**. This reader turns a folder of Markdown
files into a browsable documentation site — with a pop-out tree of every
document, a per-page table of contents, automatic section numbers, a day and
night theme, and a map of how the documents relate.

You are reading a document right now. The pane on the *left* is this page's
table of contents; the numbers you see were added automatically — they are not
written in the Markdown.

## How to navigate

There are three ways to move around, and they are deliberately different.

### The document tree

Open the hamburger button (top-left) to see every document in the library,
grouped by folder. This moves you *between* documents.

### The table of contents

The left pane lists the headings of the page you are reading. It moves you
*within* the current document.

### The map

The map button (top-right) opens a visual graph of how documents connect,
based on each page's metadata. More on that in the next document.

## What to read next

When you finish a page, the footer offers a suggested next document. Head to
the graph model to see how those suggestions are wired up.
