# Dev-only test dependencies for the Web Document Tool.
# Playwright is used purely through its Python binding, so no Node is involved in
# running the tests. The repository does carry a build-time-only package.json
# (typescript, and later the Svelte toolchain); the runtime dependencies object
# stays empty and serve.py stays Python-standard-library-only.
pytest
pytest-playwright
